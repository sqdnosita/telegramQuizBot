[project]
name = "telegram-quiz-bot"
version = "0.1.0"
description = "Telegram bot for creating and taking quizzes"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aiogram>=3.22.0",
    "aiosqlite>=0.21.0",
    "pydantic-settings>=2.0.0",
]
